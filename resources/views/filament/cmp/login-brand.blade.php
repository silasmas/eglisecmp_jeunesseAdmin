{{-- Accroche formulaire de connexion (charte portail public) --}}
<div class="cmp-login-brand">
  <p class="cmp-login-brand__eyebrow">{{ __('Département Jeunesse') }}</p>
  <p class="cmp-login-brand__title">{{ __('Connexion administration') }}</p>
  <p class="cmp-login-brand__text">
    {{ __('Gestion des inscriptions, activités et suivi des jeunes de l\'église.') }}
  </p>
</div>
